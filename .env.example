# Database connection string
DATABASE_URL=postgresql://teleuser:PASSWORD@127.0.0.1:5432/telemetryvault

# UDP Telemetry listener settings
UDP_HOST=0.0.0.0
UDP_PORT=20000

# HTTP Server settings
HTTP_HOST=0.0.0.0
HTTP_PORT=8000

# Session auto-close timeout in seconds (default 30s)
SESSION_IDLE_TIMEOUT=30.0
